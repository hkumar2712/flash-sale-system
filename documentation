Docker steps:

docker build -t user-service:0.1 .

docker run -p 8081:8081 user-service:0.1

curl http://localhost:8081/hello

Docker compose:

create docker-compose.yml at root level

docker compose up --build

curl http://localhost:8081/hello

cmd to use docker compose:
docker compose up 
docker compose down